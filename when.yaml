---
- name : Install httpd package on web servers
  hosts: webservers
  become: yes
  tasks:
    - name : un install latest httpd package
      yum:
        name: httpd
        state: removed
    - name: stop httpd
      systemd:
        name: httpd
        state: stopped
      
