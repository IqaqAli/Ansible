---
- name : Install httpd package on web servers
  hosts: webservers
  become: yes
  tasks:
    - name : install latest httpd package
      yum:
        name: nginx
        state: present
    -name: start nginx
       service:
         name: nginx
         state: started
         